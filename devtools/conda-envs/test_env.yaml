name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

  - "alchemlyb>=1.0"
  - alchemtest

    # Testing
  - pytest
  - pytest-cov
  - pytest-mock
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

